@model IEnumerable<IdentityRole>

<div class="row">
    <div class="col-md-12">
        <h1>User Roles</h1>
        <hr>
        <div class="text-right">
            <a href="/admin/role/create" class="btn btn-success btn-sm text-right">Create Role</a>
        </div>
        <table class="table table-bordered mt-3">
            <thead>
                <tr>
                    <td style="width: 380px;">Id</td>
                    <td>Name</td>
                    <td style="width: 150px;"></td>
                </tr>
            </thead>
            <tbody>
                @if (Model.Count() > 0)
                {
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Id</td>
                            <td>@item.Name</td>
                            <td class="text-center">

                                <a href="/admin/role/@item.Id" class="btn btn-success btn-sm mr-2">Edit</a>

                                <form action="/admin/role/delete" method="POST" style="display: inline;">
                                    <input type="hidden" name="roleId" value="@item.Id">
                                    <button type="submit" class="btn btn-danger btn-sm text-light">Delete</button>
                                </form>
                                
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <div class="alert alert-warning text-center">
                        <h3>No Roles</h3>
                    </div>
                }
            </tbody>
        </table>
    </div>
</div>    